# OS / editor noise
.DS_Store
*.log

# Build / bundle outputs
bundle.tar.gz
.opa-cache/

# Staging-only artifacts (not part of the public repo)
STAGING-NOTES.md
KICKOFF-PROMPT.md

# Session-state directories (must never be committed)
.claude/
.claude-tmp/

# 5-pass review artifacts (staging-only, not for public repo)
README-v2.md
CRITIQUE-pass*.md

# Tooling caches
.ruff_cache/

# Agent scratch space (intermediate notes, never committed)
docs/agents/.scratch/